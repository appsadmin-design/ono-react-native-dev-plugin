# Android Performance

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for Android work resolve to a real file. It covers performance concerns (main-thread blocking, RecyclerView/list virtualization, image loading, APK/bundle size) for Android app code, analogous to `standards/react-native/rn-performance.md` for React Native.

Until this is authored, Android performance review falls back to `standards/shared/*` only, which do not cover Android-specific performance concerns. Author this file — mirroring the ID-citation pattern in `standards/react-native/rn-performance.md` (e.g. stable IDs like `RN-PERF-*` that review findings can cite) — before relying on `/review-code` or `/prepare-mobile-release` for real Android performance sign-off.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
