# Compose / XML Standards

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for Android work resolve to a real file. It covers UI-framework conventions (Jetpack Compose vs. XML views, state hoisting) for Android app code, analogous to `standards/react-native/react-native-coding-standards.md` for React Native.

Until this is authored, Android reviews and implementation guidance fall back to `standards/shared/*` only, which do not cover Android-specific concerns. Author this file — mirroring the ID-citation pattern used in `standards/react-native/*.md` (e.g. stable IDs like `RN-*`, `API-*` that review findings can cite) — before relying on `/review-code`, `/analyze-feature`, or `/dev-design-start` for real Android work.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
