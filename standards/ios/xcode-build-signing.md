# Xcode Build & Signing

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for iOS release work resolve to a real file. It covers Xcode scheme, signing, and provisioning-profile conventions for iOS releases — cited by `/prepare-mobile-release` when the release includes an iOS build.

Until this is authored, iOS release validation falls back to `standards/shared/release-readiness.md` only, which does not cover Xcode-specific signing/provisioning concerns. Author this file — mirroring the ID-citation pattern used in `standards/react-native/*.md` and `standards/shared/release-readiness.md` (e.g. stable IDs like `REL-*` that checklist items can cite) — before relying on `/prepare-mobile-release` for real iOS release validation.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
