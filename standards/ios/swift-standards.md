# Swift Standards

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for iOS work resolve to a real file. It covers Swift language/style conventions (optionals, error handling, naming, SwiftLint) for iOS app code, analogous to `standards/react-native/react-native-coding-standards.md` for React Native.

Until this is authored, iOS reviews and implementation guidance fall back to `standards/shared/*` only, which do not cover iOS-specific concerns. Author this file — mirroring the ID-citation pattern used in `standards/react-native/*.md` (e.g. stable IDs like `RN-*`, `API-*` that review findings can cite) — before relying on `/review-code`, `/analyze-feature`, or `/create-dev-plan` for real iOS work.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
