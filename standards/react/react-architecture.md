# React Architecture

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for React (web) work resolve to a real file. It covers layering and folder-structure conventions for the plain React web platform, analogous to `standards/react-native/rn-architecture.md` for React Native. This is a deliberately separate module, not shared with React Native.

Until this is authored, React (web) reviews and implementation guidance fall back to `standards/shared/*` only, which do not cover web-specific architecture concerns. Author this file — mirroring the ID-citation pattern used in `standards/react-native/rn-architecture.md` — before relying on `/review-code`, `/analyze-feature`, or `/dev-design-start` for real React (web) work.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
