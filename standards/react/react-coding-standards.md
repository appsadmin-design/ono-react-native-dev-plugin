# React Coding Standards

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for React (web) work resolve to a real file. It covers TypeScript/React coding conventions (functional components, hooks, naming) for the plain React web platform, analogous to `standards/react-native/react-native-coding-standards.md` for React Native. This is a deliberately separate module, not shared with React Native, even though the two overlap heavily on JS/TS/React fundamentals — see the plugin's migration plan for why.

Until this is authored, React (web) reviews and implementation guidance fall back to `standards/shared/*` only, which do not cover web-specific concerns (including XSS/CSRF/CSP security and ARIA-based accessibility, which the current shared security/accessibility docs do not address). Author this file — mirroring the ID-citation pattern used in `standards/react-native/*.md` — before relying on `/review-code`, `/analyze-feature`, or `/dev-design-start` for real React (web) work.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
