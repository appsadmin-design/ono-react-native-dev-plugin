# React Performance

## Purpose & Scope

**Not yet authored.** This file is a structure-only placeholder, scaffolded as part of the mobile-division migration so routing and citation paths for React (web) work resolve to a real file. It covers performance concerns (re-renders, code-splitting, bundle size, Core Web Vitals) for the plain React web platform, analogous to `standards/react-native/rn-performance.md` for React Native. This is a deliberately separate module, not shared with React Native.

Until this is authored, React (web) performance review falls back to `standards/shared/*` only, which do not cover web-specific performance concerns. Author this file — mirroring the ID-citation pattern in `standards/react-native/rn-performance.md` (e.g. stable IDs like `RN-PERF-*`) — before relying on `/review-code` or `/prepare-mobile-release` for real React (web) performance sign-off.

## References

- This is a structure-only placeholder — see the plugin's migration plan/README for the intended scope of this file.
